# Blog Aggregation (Demo)

Run locally:

```bash
npm install
npm run dev
```

This scaffold uses Vite + React + TypeScript + Tailwind and a local MockApiService that persists to `localStorage` and seeds from `src/mocks/blogs.json`.
